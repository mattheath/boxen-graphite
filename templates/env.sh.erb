# Add Graphite scripts to path

export PATH=<%= scope.lookupvar('graphite::config::bindir') %>:$PATH

# Graphite config vars

export BOXEN_GRAPHITE_PORT=<%= scope.lookupvar('graphite::config::carbon::line_receiver_port') %>
export BOXEN_GRAPHITE_HOST="localhost"
